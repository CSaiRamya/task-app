<!DOCTYPE html>
<html>
<head>
  <title>Task Details</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

  <style>
    /* Add custom CSS styles */
    .description-container {
      max-height: 200px; /* Adjust the max height as needed */
      overflow-y: auto; /* Enable vertical scrolling for overflowed content */
      white-space: normal; /* Allow text to wrap within the container */
      word-wrap: break-word; /* Wrap long words or URLs */
    }
    .task-container {
      background-color: #f8f9fa; /* Lighter background color */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      font-family: 'Roboto', sans-serif;
    }
  </style>
</head>
<body class="bg-secondary text-black flex justify-center items-center h-screen">
  <div class="container mt-5">
    <div class="w-50 mx-auto task-container">
      <% if (task) { %>
      <h1 class="display-4"><%= task.title %></h1>
      <div class="description-container">
        <p class="text-muted"><%= task.description %></p>
      </div>
      <p class="text-muted">
        Created At: <%= task.createdAt ? task.createdAt.toLocaleString() : 'N/A' %>
      </p>
      <p class="text-muted">
        Updated At: <%= task.updatedAt ? task.updatedAt.toLocaleString() : 'N/A' %>
      </p>

      <div class="mt-4">
        <a href="/tasks/edit/<%= task.id %>"
          class="btn btn-primary btn-lg">
          Edit Task
        </a>

        <form method="POST" action="/tasks/<%= task.id %>?_method=DELETE"
              onsubmit="return confirm('Are you sure you want to delete this task?')"
              class="d-inline">
          <button type="submit"
                  class="btn btn-danger btn-lg">
            Delete Task
          </button>
        </form>
      </div>

      <% } else { %>
      <p class="text-danger mt-4">Task not found.</p>
      <% } %>

      <a href="/"
        class="mt-4 text-primary">
        Back to Task List
      </a>
    </div>
  </div>
</body>
</html>
